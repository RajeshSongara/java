/* 
public class command {
    public static void main(String argh[])
    {
        for(int i=0;i<argh.length;i++)
        {
            System.err.println(argh[i]);
        }
    }
}
 */
public class command {

    public static void main(String argh[]) {
        int a=5;
        Integer s = Integer.valueOf("2345");
        System.err.println("k= " + s);
        for (int i = 0; i < argh.length; i++) {
            // a = a + Integer.parseInt(argh[i]);
            // Integer b = Integer.valueOf("1234");
            //System.err.println("sum is " + a);
            //System.err.println("B= " + b);
        }
        System.err.println("sum is " + a);
    }
}
